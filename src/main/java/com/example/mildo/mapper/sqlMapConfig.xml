<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <properties resource="com/example/podofarm/db/db.properties"></properties>

    <typeAliases>

        <!-- 우종호 -->
        <typeAlias type="com.example.mildo.user.UserVO" alias="userVO"/>
        <typeAlias type="com.example.mildo.study.StudyVO" alias="studyVO"/>
        <typeAlias type="com.example.mildo.code.CodeVO" alias="codeVO"/>

    </typeAliases>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <dataSource type="POOLED">
                <property name="driver" value="${driver}" />
                <property name="url" value="${url}" />
                <property name="username" value="${username}" />
                <property name="password" value="${password}" />
            </dataSource>
        </environment>

    </environments>
    <mappers>
        <mapper resource="com/example/mildo/mapper/userMapper.xml" />
        <mapper resource="com/example/mildo/mapper/codeMapper.xml" />
        <mapper resource="com/example/mildo/mapper/studyMapper.xml" />

    </mappers>
</configuration>
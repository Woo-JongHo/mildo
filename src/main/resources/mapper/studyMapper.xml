<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="study">
    <insert id="create" parameterType="studyVO">
        INSERT INTO STUDY (
                           study_id,
                           user_id,
                           study_code,
                           study_name,
                           study_password,
                           study_start,
                           study_end)
        VALUES (nextval('user_number_seq'), #{userId}, #{studyCode}, #{studyName}, #{studyPassword}, #{studyStart},
                #{studyEnd})
    </insert>

</mapper>

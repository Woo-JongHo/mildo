<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="code">
    <insert id="dummyCode" parameterType="CodeVo">
        INSERT INTO CODE (
        user_id,
        ,
        code_title,
        code_level,
        code_problemid
        )
        VALUES (#{userId}, 1, 'code_title', 'code_level', 1)
    </insert>

    <!-- 날짜별 푼 문제 -->
    <select id="getSolvedByDaySelectedMonth" resultType="java.util.Map" parameterType="java.lang.String">
        WITH date_count AS (
        SELECT TO_CHAR(c_date, 'YYYY-MM-DD') AS c_date, COUNT(id) AS solved
        FROM code
        WHERE id = #{user_id}
        AND TO_CHAR(code_solvedate, 'YYYY-MM') = #{code_solvedate}
        GROUP BY TO_CHAR(c_date, 'YYYY-MM-DD')
        )
        SELECT c_date, SUM(solved) AS solved
        FROM date_count
        GROUP BY c_date
        ORDER BY c_date ASC
    </select>

    <insert id="saveComment">
        INSERT INTO COMMENT (
        code_id,
        user_id,
        comment_content,
        comment_date
        )
        VALUES (#{codeId}, #{userId}, #{commentContent}, NOW())
    </insert>

</mapper>
